{
  "name": "slukhai",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "prepare:web": "mkdir -p dist && cp index.html styles.css updates.css app.js manifest.webmanifest icon.svg sw.js sw-v2.js dist/",
    "native:sync": "npm run prepare:web && npx cap sync",
    "android": "npm run native:sync && npx cap open android",
    "ios": "npm run native:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor-community/text-to-speech": "^8.0.2",
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
